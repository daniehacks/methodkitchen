<h1><%= @recipe.title %></h2>

<h3>Ingredients</h3>

  <ul>
    <%= @recipe.ingredients.each do |ingredient| %>
      <li>
        <%= ingredient.quantity %> |
        <%= ingredient.description %>
      </li>
    <% end %>
  </ul>

<%= link_to 'Edit Title', edit_recipe_path(@recipe) %> |
<%= link_to 'Back', recipes_path %>